{
  "name": "design-system",
  "version": "1.0.0",
  "packageManager": "npm@10.9.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.3.1"
  }
}
